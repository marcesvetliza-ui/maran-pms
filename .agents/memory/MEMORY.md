- [Radix SelectItem empty value crashes](radix-select-empty-values.md) — any SelectItem with `value=""` crashes the whole React tree; dynamic sources from API can silently introduce empty strings. All known occurrences fixed as of May 2026.
- [Restaurant close dialog billing refactor](restaurant-billing-refactor.md) — Forma de Cobro (payment) is separate from Comprobante (document type); cuenta_habitacion is a payment method, not a receipt type; AFIP invoice emits when receipt is factura_a/b/c.
- [Restaurant reservations module](restaurant-reservations-complete.md) — Tab "Reservas" in restaurant.tsx is fully built: table list, status flow (pending→confirmed→check_in→historical), GuestSearchCombobox for clientId, card guarantee fields, AdvanceDialog with voucher print, check-in applies advances to order. Schema: tableId nullable, clientId/cardLast4/cardHolder/areaId/advanceAmount+Method+Date+Notes all exist. Backend: /api/restaurant/table-reservations CRUD + /advances routes all done.
- [Guest profile legal fields](guest-legal-fields.md) — Guests schema extended with: estadoCivil, procedencia, nationalityCode, fechaIngresoArgentina/Salida (conditional on non-Argentina), esEmpresaGrande (bool), montoBaseFce, codigoPostal. Countries table with AFIP codes seeded (63 countries). CountriesAbmPage at /admin/countries in Configuración sidebar.
- [Puntos de Venta (POS) system](pos-configs.md) — pos_configs table, ABM at /pos-configs, puntoVentaOverride in NewInvoiceData; selector appears in billing EmitirFacturaDialog and restaurant close dialog (factura_a/b/c only); seeded 4 default PVs.
