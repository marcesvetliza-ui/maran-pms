Perfecto. Ahora integro el nuevo `main` (con los fixes de seguridad ya mergeados) en `feature/pilot-environment` mediante un merge normal, como acordamos.

Ahora reviso el segundo archivo en conflicto, `server/debug-assets-routes.ts`.

Ahora `server/index.ts` y `server/routes.ts`.

Keep HEAD (pilot) version — it's the superset. Resolving both conflicts in `server/index.ts`.

Ahora `server/routes.ts`.

Keep HEAD (it already imports `registerAuthBootstrapRoute` plus the pilot-specific extras).

This region is clean and correct — no further conflicts here. Now let's check the two test files.

Ahora el archivo de tests de auth-bootstrap.

No quedan marcadores de conflicto. Reviso rápidamente los archivos que se auto-mergearon sin conflicto, para confirmar que quedaron coherentes.

Correcto — cambio limpio, sin conflicto. Ahora marco todos los archivos como resueltos y valido con TypeScript/tests antes de commitear.

TypeScript compila limpio. Ahora corro los tests relevantes.

Verde. Ahora corro toda la suite de servidor (mocked) y, contra mi Postgres local, la suite de integración real.

Local Postgres del sandbox sigue disponible. Corro la suite completa con `DATABASE_URL` configurada.

Migraciones OK. Ahora corro la suite completa de server + postgres contra este esquema.

Server suite completa: 528/528 verde. Ahora la suite de PostgreSQL real (incluida la de concurrencia del bootstrap fusionada).

Excelente — 85/85 (antes eran 84; ahora suma la nueva prueba de concurrencia del bootstrap, ya integrada). Corro también la suite de cliente antes de commitear.

Todo verde: cliente 219/219, servidor 528/528, postgres 85/85. Confirmo que no quedan cambios sin commitear en el árbol y hago el merge commit.

Todo en orden. Confirmo el estado del merge y creo el commit.

Merge commit creado. Ahora hago push a `feature/pilot-environment`.

Listo. Hice el merge de `main` en `feature/pilot-environment` con un merge normal (no rebase), resolví manualmente los conflictos reales que surgieron —ambas ramas habían tocado los mismos archivos en paralelo (`server/auth-bootstrap.ts`, `server/debug-assets-routes.ts`, `server/index.ts`, `server/routes.ts` y sus tests)— y pusheé.

**Cómo reconcilié los conflictos** (no era un merge trivial, ambas ramas resolvieron el mismo problema por separado):
- **`auth-bootstrap.ts`**: combiné los dos endurecimientos. De `main`: gate explícito `ADMIN_BOOTSTRAP_ENABLED`, longitud mínima de secreto, atomicidad con `pg_advisory_xact_lock`, logging sin exponer el error crudo. De `feature/pilot-environment`: el veto adicional por `APP_ENV` (deshabilitado también en el ambiente piloto, no solo en producción). El resultado exige las dos condiciones y respeta los tres vetos.
- **`debug-assets-routes.ts` / `index.ts` / `routes.ts`**: mantuve la estructura de la rama piloto (bloquea explícitamente al rol `piloto_externo`), que ya cubre el fix de `main` (ambas rutas exigen sesión).
- **Tests**: fusioné los casos de las dos rondas de revisión de seguridad con los casos específicos de piloto, y ajusté la nueva prueba de concurrencia (`auth-bootstrap-concurrency.pg.test.ts`, que vino de `main`) para que inicialice `APP_ENV` correctamente, ya que importa `routes.ts` directo sin pasar por `server/index.ts`.

**Validé todo localmente contra Postgres real antes de pushear**: cliente 219/219, servidor 528/528, PostgreSQL 85/85 (la suite de postgres pasó de 84 a 85 tests, incorporando la prueba de concurrencia).

Con esto, las condiciones que fijaste para el PR #2 quedan completas: mergeado, CI verde, `main` desplegado, y ahora integrado en `feature/pilot-environment`. Volví a mi rama asignada (`claude/hotel-system-replit-ke42ze`) para no dejar trabajo pendiente ahí.